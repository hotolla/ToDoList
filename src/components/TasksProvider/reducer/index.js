import { filterReducer } from './filterReducer';
import { loadingReducer } from './loadingReducer';
import { taskReducer } from './taskReducer';

export const reducer = { filterReducer, loadingReducer, taskReducer };
